#include <stdio.h>
#include "main.h"

/**
* mul - a C function that multiplies two integers
* @x: first integer to be multiplied
* @y: second integer to be multiplied
* Return: addition of x and y
*/

int mul(int x, int y)
{
	return (x * y);
}
